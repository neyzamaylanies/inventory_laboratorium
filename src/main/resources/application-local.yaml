spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory?useSSL=false&serverTimezone=Asia/Jakarta 
    username: root
    password:

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true            # tampilkan query di console (dev only)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true

server:
  port: 8080
  address: 0.0.0.0